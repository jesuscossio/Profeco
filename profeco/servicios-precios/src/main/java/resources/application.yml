server:
  port: 8082

spring:
  application:
    name: servicio-precios

  datasource:
    url: jdbc:mysql://localhost:3306/precios_db
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    org.hibernate.SQL: DEBUG